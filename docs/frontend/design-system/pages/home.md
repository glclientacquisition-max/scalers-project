# Home `/home`

**Job:** What needs me, what happened, is the line working, one next action.  
**Visual:** Follow [`MASTER.md`](../MASTER.md). Do not invent a dashboard.

## Now

Command Center for new call leads.

Header hierarchy: greeting is an uppercase eyebrow (`nairobiGreeting()`), business name is the `h1` (Sora, clamp 1.5–2rem), Nairobi calendar day is a `<time>` caption (`nairobiDateLabel()`). No instructional subtitle.

Optional Updates strip when `liveBulletinItems` has rows. Bulletin ping stays on Updates only.

8/4 split: new-lead queue + one aside surface (`border-line` / `bg-surface`) that groups activity counts, Line, and the docked primary CTA. Compact today / followed-up links, not four KPI tiles.

Line live / Number pending / Needs training from DID + `assessMvpAnswerReadiness` (`ready` + first required gap, never `score`). Allowed line copy only. Never Online.

One derived primary CTA (`homeNextAction`): Process leads / Test assistant / Teach assistant / Add credit / Show all calls.

`TriageLeadCard` is a dense divided row (`px-4 py-3`). Hover and focus-within wash `#0096FF` at 4% plus a 2px left accent. WhatsApp / Open keep hover, active, and the canonical focus ring. Archive / Done stay muted icon actions.

Wallet low only when prepaid is actually under threshold. No `max-w-3xl`. Dead `all` / `done` / `archived` / 40-lead fetches removed.

Density stays 8. Do not double page padding or import landing-scale type, glass, mesh, or glow.

## Empty

If no new leads, Calls empty-state language (`No new call leads`) plus the one derived next action in the aside. No illustration.

## Later

Do not mix requests/appointments into the Home queue until those lists share Calls language.
