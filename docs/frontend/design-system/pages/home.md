# Home (Command Center)

**Route:** `/home`  
**Job:** At 08:00 EAT, answer four chunks only.

1. **Is it working** — Line live / Number pending / Needs training. Wallet warn if KES < 200.
2. **What needs me** — Three Inbox destinations: Needs you, Holds, Jobs. Caption is a briefing (`2 to confirm. 1 to fulfill.`). When a queue is 1, the unit is the actual job (visit time, item).
3. **What happened** — Calls today as a compact link.
4. **What to do next** — Confirm visit, Fulfill hold, Return call, Train, or Test line. CTA opens the sharpest queue.

**Chrome:** Greeting eyebrow, workspace name as `h1`, Nairobi `<time>`. One aside: today + line + docked CTA.

**Data (real only):** tenant row, Nairobi-day call count, new leads, open `service_requests`, requested/confirmed `appointments`, one oldest open hold, one oldest pending job, `assessMvpAnswerReadiness`, live bulletin.

**Do not:** duplicate the Inbox table. Show Online. Use stacked `TriageLeadCard`.
