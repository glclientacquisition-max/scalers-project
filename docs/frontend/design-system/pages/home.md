# Home (Command Center)

**Route:** `/home`  
**Job:** At 08:00 EAT, answer four chunks only.

1. **Is it working** — Line live / Number pending / Needs training. Wallet warn if KES < 200.
2. **What needs me** — Three queues: Calls, Requests, Appointments. Counts only. Each row opens that list.
3. **What happened** — Calls today and followed-up as compact links.
4. **What to do next** — One primary CTA to the hottest queue, or Train / Test line.

**Chrome:** Greeting eyebrow, workspace name as `h1`, Nairobi `<time>`. One aside: today + line + docked CTA.

**Data (real only):** tenant row, Nairobi-day call count, `lead_status=new`, open `service_requests`, requested `appointments`, `assessMvpAnswerReadiness`, live bulletin.

**Do not:** duplicate the Calls table. Show Online. Use stacked `TriageLeadCard`. Invent a fourth inbox.
