# Settings

**Route:** `/settings`  
**Role:** Preserve the existing Train sidebar IA.

Category titles stay non-clickable (`uppercase tracking-wide text-ink-soft`). Sticky Save stays under `--desk-header-h`. Deep links: `businessSettingsHref(tab)`.
