# Super Admin

**Route:** `/admin`  
**Job:** Ops console for wallets, businesses, numbers, and voices.

**Chrome exception:** Owner desk bans a left rail. Super Admin keeps a fixed navy sidebar on `lg+` (horizontal nav below `lg`). Do not copy this shell onto `(desk)`.

Admin stays light. Page titles use `text-2xl`. Filled primaries use `bg-accent-fill text-accent-on-fill`.
